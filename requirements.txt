

# Dépendances système (optionnelles)
psutil>=5.9.0  # Pour des fonctionnalités système avancées

# Interface utilisateur améliorée (optionnel)
pillow>=9.0.0  # Pour les opérations sur les images, nécessaire pour pystray
pystray>=0.19.0  # Pour l'icône dans la barre des tâches

# Compatibilité multi-plateforme pour les notifications
plyer>=2.0.0  # Pour les notifications système multi-plateformes
# Dépendances optionnelles
pytest>=6.0.0
pytest-cov>=2.12.0
sphinx>=4.0.0
sphinx_rtd_theme>=0.5.0
black>=21.5b0
flake8>=3.9.0
